name: Second

on:
  workflow_run:
    workflows: ["First"]
    types:
      - completed

jobs:
  test2:
    name: test
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: alibaba/cloud-native-test-ci-tool@9390ae2dface06e42dd327a436c3c514a16b74ed
        name: Deploy rocketmq
        with:
          action: "try"
          ask-config: "${{ secrets.ASK_CONFIG }}"
